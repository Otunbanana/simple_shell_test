#include "shs.h"
/**
* main - check the code
* @ac: count.
* @agv: argument vector.
* @env: environment vector.
* Return: Always 0.
*/
int main(int ac, char **agv, char **env)
{
if (ac == 1)
first_file(agv, env);
return (0);
}
